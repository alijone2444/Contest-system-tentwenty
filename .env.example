# PostgreSQL connection string (Neon / Supabase / local Postgres)
# Example (local):  postgresql://postgres:postgres@localhost:5432/contest_system
# Example (Neon):   postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/contest_system"

# Secret used to sign JWTs — use a long random string in production
JWT_SECRET="change-me-to-a-long-random-secret"

# Shown in the site header
NEXT_PUBLIC_GITHUB_URL="https://github.com/your-username/contest-system"
NEXT_PUBLIC_CONTACT_EMAIL="annassalman.98@gmail.com"
